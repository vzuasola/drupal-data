{#
/**
 * @file webcomposer_slider_entity.html.twig
 * Default theme implementation to present Webcomposer slider entity data.
 *
 * This template is used when viewing Webcomposer slider entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_webcomposer_slider_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('webcomposer_slider_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>

parameters:
  monolog.channel_handlers:
    default: ['file', 'drupal.dblog']
    php: ['file', 'drupal.dblog']

services:
  monolog.handler.file:
    class:  Drupal\webcomposer_monolog\Monolog\Handler\StreamHandler
    arguments: ['200', false]

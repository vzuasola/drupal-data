{#
/**
 * @file social_media_entity.html.twig
 * Default theme implementation to present Social Media data.
 *
 * This template is used when viewing Social Media pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_social_media_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('social_media_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>

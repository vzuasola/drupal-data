{#
/**
 * @file right_side_block_entity.html.twig
 * Default theme implementation to present Inner Page Right Side Block data.
 *
 * This template is used when viewing Inner Page Right Side Block pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_right_side_block_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('right_side_block_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>

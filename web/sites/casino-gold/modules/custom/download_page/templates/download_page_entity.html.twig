{#
/**
 * @file download_page_entity.html.twig
 * Default theme implementation to present Download page entity data.
 *
 * This template is used when viewing Download page entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_download_page_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('download_page_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>

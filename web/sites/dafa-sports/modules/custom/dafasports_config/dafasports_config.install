<?php

/**
 * Implements hook_uninstall
 */
function dafasports_config_uninstall() {
  $keys = [
    'dafasports_config.dafasports_configuration',
    'rest.resource.dafasports_config_resource',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

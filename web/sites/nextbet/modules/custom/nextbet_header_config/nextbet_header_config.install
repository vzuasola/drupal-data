<?php


/**
 * Implements hook_uninstall
 */
function nextbet_header_config_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.header',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

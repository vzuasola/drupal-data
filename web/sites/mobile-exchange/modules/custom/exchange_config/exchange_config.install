<?php

/**
 * @file
 * Filesource Uninstall the configs.
 */

/**
 * Implements hook_uninstall().
 */
function exchange_config_uninstall() {
  $keys = [
    'exchange_config.exchange_configuration',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

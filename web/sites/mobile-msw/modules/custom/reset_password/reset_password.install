<?php

/**
 * Implements hook_uninstall
 */
function reset_password_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.reset_password'
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

<?php

/**
 * Implements hook_uninstall
 */
function forgot_password_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.forgot_password'
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

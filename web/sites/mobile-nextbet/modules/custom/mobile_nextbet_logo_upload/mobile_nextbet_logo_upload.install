<?php


/**
 * Implements hook_uninstall
 */
function nextbet_logo_upload_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.logo_upload',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

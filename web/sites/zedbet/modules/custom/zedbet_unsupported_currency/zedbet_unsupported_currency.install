<?php


/**
 * Implements hook_uninstall
 */
function zedbet_unsupported_currency_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.unsupported_currency',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

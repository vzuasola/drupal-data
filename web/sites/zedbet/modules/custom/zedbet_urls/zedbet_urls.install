<?php


/**
 * Implements hook_uninstall
 */
function zedbet_urls_uninstall() {
  $keys = [
    'webcomposer_form_manager.form.urls',
  ];

  foreach ($keys as $key) {
    \Drupal::configFactory()->getEditable($key)->delete();
  }
}

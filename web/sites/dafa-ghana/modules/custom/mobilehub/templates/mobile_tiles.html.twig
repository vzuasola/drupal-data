{#
/**
 * @file mobile_tiles.html.twig
 * Default theme implementation to present Mobile tiles data.
 *
 * This template is used when viewing Mobile tiles pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_mobile_tiles()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('mobile_tiles') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>

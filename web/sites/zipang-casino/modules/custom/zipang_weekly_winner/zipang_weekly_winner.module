<?php

/**
 * @file
 * Contains zipang_weekly_winner.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function zipang_weekly_winner_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the zipang_weekly_winner module.
    case 'help.page.zipang_weekly_winner':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('List of Zipang Weekly Winner entities') . '</p>';
      return $output;

    default:
  }
}
